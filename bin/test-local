#!/usr/bin/env bash

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repo_dir="$(dirname "$script_dir")"

if [ "${1:-}" = "--help" ]; then
	printf 'Usage: %s [rojo args ...]\n' "$0"
	exit 0
fi

exec rojo serve "$repo_dir/test.project.json" "$@"
